/**
 * Auth cookie configuration constants
 */

/** Prefix for all auth cookies */
export const AUTH_COOKIE_PREFIX = 'plenus'

/** Session cookie name (without prefix) */
export const AUTH_COOKIE_SESSION_NAME = 'session'

/** Last login method cookie name (without prefix) */
export const AUTH_COOKIE_LAST_LOGIN_METHOD_NAME = 'last_login_method'
